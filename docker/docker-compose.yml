version: '3'

services:
  omnik:
    build: .
    image: omnikdatalogger:latest
    volumes:
      - "${PWD}/config.ini:/config.ini"
    expose:
      - "10004"
    logging:
      driver: json-file
      
